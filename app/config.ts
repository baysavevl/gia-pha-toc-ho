const config = {
  siteName: process.env.SITE_NAME || "Hồ Văn Tộc",
  exampleEmail: process.env.EXAMPLE_EMAIL || "",
  examplePassword: process.env.EXAMPLE_PASSWORD || "",
  demoDomain: process.env.DEMO_DOMAIN || "demo.hovantoc.example",
};

export default config;
